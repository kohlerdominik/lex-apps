kind: Service
apiVersion: v1
metadata:
  name: {{ template "openvpn.fullname" . }}
  labels:
    app: {{ template "openvpn.name" . }}
    chart: {{ template "openvpn.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- if .Values.service.annotations }}
  annotations:
{{ toYaml .Values.service.annotations | indent 4 }}
{{- end }}
spec:
{{- if .Values.service.externalIPs }}
  externalIPs:
{{ toYaml .Values.service.externalIPs | indent 4 }}
{{- end }}
  ports:
    - name: openvpn
      port: {{- printf "%s0" .Values.service.portOffset -}}
      targetPort: {{ .Values.service.internalPort }}
      protocol: {{ .Values.openvpn.OVPN_PROTO | upper }}
    - name: forward1_tcp
      port: {{- printf "%s1" .Values.service.portOffset -}}
      targetPort: 9901
      protocol: TCP
    - name: forward1_udp
      port: {{- printf "%s1" .Values.service.portOffset -}}
      targetPort: 9901
      protocol: UDP
    - name: forward2_tcp
      port: {{- printf "%s2" .Values.service.portOffset -}}
      targetPort: 9902
      protocol: TCP
    - name: forward2_udp
      port: {{- printf "%s2" .Values.service.portOffset -}}
      targetPort: 9902
      protocol: UDP
    - name: forward3_tcp
      port: {{- printf "%s2" .Values.service.portOffset -}}
      targetPort: 9903
      protocol: TCP
    - name: forward3_udp
      port: {{- printf "%s2" .Values.service.portOffset -}}
      targetPort: 9903
      protocol: UDP
  selector:
    app: {{ template "openvpn.name" . }}
    release: {{ .Release.Name }}
  type: {{ .Values.service.type }}
{{- if .Values.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.service.loadBalancerIP }}
{{- end }}
